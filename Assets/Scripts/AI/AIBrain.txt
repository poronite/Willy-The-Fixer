tree("Root")
	tree("AI")

tree("AI")
	fallback
		tree("isHiding")
		tree("HideFromPlayer")
		tree("GoToTarget")

tree("isHiding")
	fallback
		isHiding
		

tree("HideFromPlayer")
	sequence
		hasSeenPlayer
		race
			HideFromPlayer
			sequence
				Wait 10.0
				StopRunningAway
				LookBehind
				while
					not isSeeing
					sequence
						Wait 15.0
						ForgetPlayer
				

tree("GoToTarget")
	sequence
		GoToTarget

